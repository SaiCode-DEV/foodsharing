/**
 * Equivalent to PHP class Foodsharing\Modules\Core\DBConstants\Voting\VotingType
 */
enum VotingType {
  SELECT_ONE_CHOICE = 0,
  SELECT_MULTIPLE = 1,
  THUMB_VOTING = 2,
}

export default VotingType;
