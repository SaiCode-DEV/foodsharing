<div class="border">
<p>{{ MESSAGE|raw }}</p>
    {# the colon belongs inside the anchor: the plain text part appends the url behind it #}
    <p> {{ 'email_template.from'|trans }}<a href="{{ USERPROFILE }}">{{ USERNAME }}:</a></p>
</div>