⚠ {{ 'email_template.warning'|trans }}: 
⚠ {% if STORES|length == 1 %}
  {{ 'email_template.fetch_warning.subject.single'|trans({'%BETRIEB%': STORES[0].name}) }}
{% else %}
  {{ 'email_template.fetch_warning.subject.multiple'|trans({'%COUNT%': STORES|length}) }}
{% endif %}
