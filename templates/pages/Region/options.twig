{% extends 'pages/Region/baseRegion.twig' %}

{% block main %}
    {% include 'partials/vue-wrapper.twig' with {
        id: 'vue-options',
        component: 'options',
        props: {
            regionId : region.id,
            regionName : region.name,
            maySetRegionOptionsReportButtons : maySetRegionOptionsReportButtons,
            maySetRegionOptionsRegionPickupRule : maySetRegionOptionsRegionPickupRule,
            isReportButtonEnabled : isReportButtonEnabled,
            isMediationButtonEnabled : isMediationButtonEnabled,
            isRegionPickupRuleActive : isRegionPickupRuleActive,
            regionPickupRuleTimespanDays : regionPickupRuleTimespanDays,
            regionPickupRuleLimitNumber : regionPickupRuleLimitNumber,
            regionPickupRuleLimitDayNumber : regionPickupRuleLimitDayNumber,
            regionPickupRuleInactiveHours : regionPickupRuleInactiveHours,
            regionPickupRuleActiveStoreList : regionPickupRuleActiveStoreList,
            reportReasonOptions : reportReasonOptions,
            isReportReasonOtherEnabled : isReportReasonOtherEnabled
        }
    } %}
{% endblock %}
